import Vue from "vue";
import Router from "vue-router";
Vue.use(Router);

const router =  new Router({
    // routes: [
    //     {
    //         path: '/',
    //         redirect: { name: 'login' }
    //     }
    // ]
});
//
// router.beforeEach((to, from, next) => {
//     next(store.commit('updateRotaStore', to.name));
// });
export default router
